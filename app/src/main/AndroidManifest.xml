<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.aya">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".parent" />
        <activity android:name=".student" />
        <activity android:name=".ParentBody" />
        <activity android:name=".ParentProfile" />
        <activity android:name=".activiries" />
        <activity android:name=".parent_act" />
        <activity android:name=".parent_grades" />
        <activity android:name=".parent_att" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".StudentBody" />
        <activity android:name=".StudentProfile" />
        <activity android:name=".grades_s" />
        <activity android:name=".activity_S" />
        <activity android:name=".attendance_s" />
        <activity android:name=".grades_1" />
        <activity android:name=".aya_p"></activity>
    </application>

</manifest>